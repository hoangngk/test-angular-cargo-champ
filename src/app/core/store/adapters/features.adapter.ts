// import { createEntityAdapter, EntityAdapter } from '@ngrx/entity';

// import { Feature } from '../../interfaces';

// export const adapter: EntityAdapter<any> = createEntityAdapter<any>({
//   selectId: (feature: any) => {
//     return feature._id;
//   },
// });
